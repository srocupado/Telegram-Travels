name: telegram-travels

services:
  bot:
    build: .
    image: telegram-travels:local
    restart: unless-stopped
    env_file: .env
    volumes:
      - travels_data:/data

volumes:
  travels_data:
